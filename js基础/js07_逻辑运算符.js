/* 
逻辑或 ||
    计算每一个操作数的值，将返回第一个真值，如果不存在真值，就返回最后一个值。
*/
var demo = 1 < 2 || 2 > 3;

/* 
逻辑与 &&
    计算每一个操作数的值，将返回第一个假值，如果不存在假值，就返回最后一个值。
*/
var demo = 1 < 2 && 2 > 3;

/* 
!（非）
    步骤一：将操作数转化为布尔类型：true/false；
    步骤二：返回相反的值；
*/

// 两个非运算 !! 有时候用来将某个值转化为布尔类型：
var flag = 200
flag = !!flag // true